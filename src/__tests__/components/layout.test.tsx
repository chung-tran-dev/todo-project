import { render, screen } from '@testing-library/react'
// import userEvent from '@testing-library/user-event'
import '@testing-library/jest-dom'
import { TodoLayout } from '../../components/layout/TodoLayout/TodoLayout'

test('display layout with an child component', async () => {
    // render(<TodoLayout><div>children</div></TodoLayout>);
    // // ASSERT
    // expect(true).toEqual(true);
})